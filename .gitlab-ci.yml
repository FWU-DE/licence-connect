stages:
#  - build
  - pre_test
  - test
  - docker
#  - release

variables:
  IMAGE_NAME: lc-core
  MAVEN_IMAGE: maven:3-amazoncorretto-21
  TEST_IMAGE_NAME: corretto-with-python

  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository -Djava.awt.headless=true"
  MAVEN_CLI_OPTS: "--settings mvn_ci_settings.xml --batch-mode --errors --fail-at-end"
  MAVEN_CLI_TEST_OPTS: "-Dspring.profiles.active=auto-start-mocks"

  LATEST_IMAGE_TAG: "$CI_REGISTRY/licenceconnect/API/$IMAGE_NAME:latest"
  RELEASE_IMAGE_TAG: "$CI_REGISTRY/licenceconnect/API/$IMAGE_NAME:$CI_COMMIT_TAG"

cache:
  paths:
    - ".m2/repository"

default:
  tags:
    - docker

#build:
#  stage: build
#  image: $MAVEN_IMAGE
#  script:
#    - mvn $MAVEN_CLI_OPTS compile
#  artifacts:
#    paths:
#      - target/
#    expire_in: 30 min

create_test_image:
  image: docker:27.2.0-rc.1-cli
  stage: pre_test
  script:
    - echo "$CI_JOB_TOKEN" | docker login $CI_REGISTRY -u gitlab-ci-token --password-stdin
    - docker build . -f src/test/resources/Dockerfile -t $TEST_IMAGE_NAME
    - docker push $TEST_IMAGE_NAME

run_tests:
  image: $TEST_IMAGE_NAME
  stage: test
  script:
    - mvn --version
#    - mvn $MAVEN_CLI_OPTS test $MAVEN_CLI_TEST_OPTS

docker_build_latest:
  image: $MAVEN_IMAGE
  stage: docker
  script:
    - mvn $MAVEN_CLI_OPTS spring-boot:build-image
      -DskipTests=true
      -Ddocker.image.name=$LATEST_IMAGE_TAG
#      -Ddocker.registry.username=gitlab-ci-token
#      -Ddocker.registry.password=$CI_JOB_TOKEN
#      -Ddocker.registry.url=$CI_REGISTRY
#      -Ddocker.publish=true
  only:
    - develop

#docker_release_tag:
#  stage: release
#  script:
#    - mvn $MAVEN_CLI_OPTS spring-boot:build-image
#      -DskipTests=true
#      -Ddocker.image.name=$RELEASE_IMAGE_TAG
#      -Ddocker.registry.username=gitlab-ci-token
#      -Ddocker.registry.password=$CI_JOB_TOKEN
#      -Ddocker.registry.url=$CI_REGISTRY
#      -Ddocker.publish=true
#  only:
#    - tags
